This script will be able to upload directory recursive. Using swift api. 

Require:

php5-cli
php5-curl
swift:
	sudo add-apt-repository ppa:swift-core/release
	sudo apt-get update
	sudo apt-get -y install python-software-properties swift
